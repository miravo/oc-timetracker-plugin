columns:
    worker[name]:
        label: Worker name
        type: text
    worker[company][name]:
        label: Worker company name
        type: text
    check_in_time:
        label: Check in time
        type: text
    check_out_time:
        label: Check out time
        type: text
    created_at:
        label: Created at
        type: datetime
    updated_at:
        label: Updated at
        type: datetime
